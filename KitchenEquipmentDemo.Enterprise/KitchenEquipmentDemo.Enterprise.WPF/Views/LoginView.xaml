<UserControl x:Class="KitchenEquipmentDemo.Enterprise.WPF.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:h="clr-namespace:KitchenEquipmentDemo.Enterprise.WPF.Helpers">
    <!-- Fill the content area and center the login card -->
    <Grid Background="{StaticResource Brush.White}">
        <Border Background="{StaticResource Brush.White}"
            BorderBrush="{StaticResource Brush.Accent}"
            BorderThickness="1"
            CornerRadius="12"
            Padding="24"
            Width="420"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.25"/>
            </Border.Effect>

            <StackPanel>
                <!-- Logo & title -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <Image Source="/Assets/logo.png" Height="56" Margin="0,0,12,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Duke Kitchen Equipment" 
                       FontSize="18" FontWeight="SemiBold"
                       Foreground="{StaticResource Brush.Text}"/>
                        <TextBlock Text="Log in to continue"
                       Foreground="{StaticResource Brush.Text}"
                       Opacity="0.8"/>
                    </StackPanel>
                </StackPanel>

                <!-- Username -->
                <StackPanel Margin="0,6">
                    <TextBlock Text="Username" Margin="2,0,0,4"/>
                    <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <!-- Password (bound via helper) -->
                <StackPanel Margin="0,6">
                    <TextBlock Text="Password" Margin="2,0,0,4"/>
                    <PasswordBox
              h:PasswordBoxHelper.BindPassword="True"
              h:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <!-- Actions -->
                <DockPanel Margin="0,16,0,0" LastChildFill="False">
                    <!-- SignUp on left -->
                    <Button Content="SignUp"
            Command="{Binding SignUpCommand}"
            Style="{StaticResource BtnAccent}"
            Width="120"
            DockPanel.Dock="Left"/>

                    <!-- Login on right -->
                    <Button Content="Login"
            Command="{Binding LoginCommand}"
            Style="{StaticResource BtnAccent}"
            Width="120"
            DockPanel.Dock="Right"
            IsDefault="True"/>
                </DockPanel>

                <!-- Message -->
                <TextBlock Text="{Binding Message}"
                   Foreground="Tomato"
                   Margin="0,10,0,0"/>

                <!-- Hint -->
                <TextBlock Margin="0,8,0,0" Foreground="{StaticResource Brush.Text}" Opacity="0.8">
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
