<UserControl x:Class="KitchenEquipmentDemo.Enterprise.WPF.Views.SiteFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource Brush.White}" Padding="0,0,0,12">
            <TextBlock Text="{Binding ScreenName}"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource Brush.Deep}"
                       FontSize="20"
                       FontWeight="SemiBold"/>
        </Border>

        <!-- Content Area -->
        <Border Grid.Row="1" Background="{StaticResource Brush.White}" 
                BorderThickness="1" BorderBrush="#EEEEEE"
                CornerRadius="4" Padding="0">

            <!-- Tabs -->
            <TabControl>
                <!-- Site Info Tab -->
                <TabItem Header="Site Information">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Scrollable Form -->
                        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="40,20,40,20" 
                                        Width="800" 
                                        HorizontalAlignment="Left">
                                <TextBlock Text="Site Code *" Margin="0,0,0,5" FontWeight="SemiBold"/>
                                <TextBox Margin="0,0,0,15" Height="30"
                                         Text="{Binding Code, UpdateSourceTrigger=PropertyChanged}"/>

                                <TextBlock Text="Site Name *" Margin="0,0,0,5" FontWeight="SemiBold"/>
                                <TextBox Margin="0,0,0,15" Height="30"
                                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

                                <TextBlock Text="Description" Margin="0,0,0,5" FontWeight="SemiBold"/>
                                <TextBox Margin="0,0,0,15" Height="60"
                                         Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                         TextWrapping="Wrap" AcceptsReturn="True"
                                         VerticalScrollBarVisibility="Auto"/>

                                <CheckBox Content="Active Site"
                                          IsChecked="{Binding Active}"
                                          Margin="0,0,0,20"/>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Button Panel -->
                        <Border Grid.Row="1" Background="#F9F9F9" 
                                BorderThickness="0,1,0,0" BorderBrush="#EEEEEE"
                                Padding="10">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="Cancel"
                                        Command="{Binding CancelCommand}"
                                        Width="100" Height="32"
                                        Margin="0,0,10,0"
                                        Style="{StaticResource BtnAccent}"/>
                                <Button Content="Save Site"
                                        Command="{Binding SaveSiteCommand}"
                                        Width="120" Height="32"
                                        Style="{StaticResource BtnAccent}"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </TabItem>

                <!-- Equipment Assignment Tab -->
                <TabItem Header="Equipment Assignment">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Equipment Assignment Content -->
                        <Grid Grid.Row="0" Margin="20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Available Equipment -->
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Available Equipment" FontWeight="Bold" Margin="0,0,0,10"/>
                                <ListView ItemsSource="{Binding AvailableEquipment}"
                                          SelectedItem="{Binding SelectedAvailableEquipment}"
                                          Height="300">
                                    <ListView.View>
                                        <GridView>
                                            <GridViewColumn Header="Serial" Width="120" DisplayMemberBinding="{Binding SerialNumber}"/>
                                            <GridViewColumn Header="Description" Width="200" DisplayMemberBinding="{Binding Description}"/>
                                        </GridView>
                                    </ListView.View>
                                </ListView>
                            </StackPanel>

                            <!-- Assignment Buttons -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0,20,0">
                                <Button Content="→ Assign →"
                                        Command="{Binding AssignEquipmentCommand}"
                                        Width="100" Height="30" Margin="0,10,0,10"
                                        Style="{StaticResource BtnPrimary}"/>
                                <Button Content="← Unassign ←"
                                        Command="{Binding UnassignEquipmentCommand}"
                                        Width="100" Height="30"
                                        Style="{StaticResource BtnAccent}"/>
                            </StackPanel>

                            <!-- Assigned Equipment -->
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Assigned Equipment" FontWeight="Bold" Margin="0,0,0,10"/>
                                <ListView ItemsSource="{Binding AssignedEquipment}"
                                          SelectedItem="{Binding SelectedAssignedEquipment}"
                                          Height="300">
                                    <ListView.View>
                                        <GridView>
                                            <GridViewColumn Header="Serial" Width="120" DisplayMemberBinding="{Binding SerialNumber}"/>
                                            <GridViewColumn Header="Description" Width="200" DisplayMemberBinding="{Binding Description}"/>
                                        </GridView>
                                    </ListView.View>
                                </ListView>
                            </StackPanel>
                        </Grid>

                        <!-- Button Panel -->
                        <Border Grid.Row="1" Background="#F9F9F9" 
                                BorderThickness="0,1,0,0" BorderBrush="#EEEEEE"
                                Padding="10">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="Cancel"
                                        Command="{Binding CancelCommand}"
                                        Width="100" Height="32"
                                        Margin="0,0,10,0"
                                        Style="{StaticResource BtnAccent}"/>
                                <Button Content="Save Site"
                                        Command="{Binding SaveSiteCommand}"
                                        Width="120" Height="32"
                                        Style="{StaticResource BtnAccent}"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </TabItem>
            </TabControl>
        </Border>
    </Grid>
</UserControl>